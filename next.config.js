/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["assets.example.com"], // Add your image domains here if needed
  },
  // Other Next.js config options...
};

module.exports = nextConfig;
